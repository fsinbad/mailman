apiVersion: v1
kind: Service
metadata:
  name: {{ include "mailman.fullname" . }}-backend
  labels:
    {{- include "mailman.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
  {{- with .Values.mailman.backend.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.mailman.backend.service.type }}
  ports:
    - port: {{ .Values.mailman.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mailman.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend